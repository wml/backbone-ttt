    <% cell_size = 125 %>
    <% grid_size = 3 * cell_size %>
    <% mini_cell_size = 75 %>
    <% mini_grid_size = 3 * 75 %>

    <!-- TODO
      set overflow attr on history after enough added
      status in DB: 3 should be draw
      memoization for minimax to decrease move time
    -->

    <script type="text/javascript">      
      var Graphics = {
        mainCellSize: <%= cell_size %>,

        getMainContext: function() {
          return UI.getBoard()[0].getContext("2d");
        },

        eachRow: function(callback) {
          for (var row = 0; row < 3; ++row) {
            for (var col = 0; col < 3; ++col) {
              callback(row, col);			    
            }
          }
        },

        draw: function(state, winner) {
          this.drawState(
            this.getMainContext(), 0, 0, Graphics.mainCellSize, state, winner
          );
        },

        drawState: function(ctx, x, y, cellsize, state, winner) {
          var s = cellsize;
          var ximg = $('#x-img')[0];
          var oimg = $('#o-img')[0];
          ctx.globalAlpha = 1;

          Graphics.eachRow(function(row, col) {
            var left = x + s * col;
            var top = y + s * row;

            ctx.beginPath();
            ctx.rect(left, top, s, s);
            ctx.fillStyle = "#e8e8e8";
            ctx.fill();
            ctx.lineWidth = 1;
            ctx.strokeStyle = "#666666";
            ctx.stroke();

            if (Game.States.Human == state[row][col]) {
              ctx.drawImage(ximg, left, top);
            }
            else if (Game.States.Opponent == state[row][col]) {
              ctx.drawImage(oimg, left, top);
            }
          });

          if (null != winner) {
            if (Game.States.Tie != winner.who) {
              Graphics.drawWinner(ctx, winner);
            }

            ctx.beginPath();
            ctx.globalAlpha = 0.5;
            ctx.rect(0, 0, <%= grid_size %>, <%= grid_size %>);
            ctx.fillStyle = "#fff";
            ctx.fill();
          }
        },

        drawWinner: function(ctx, winner) {
          var s = Graphics.mainCellSize;
          var color = (Game.States.Human == winner.who ? '#0F0' : '#F00');

          ctx.beginPath();
          ctx.lineWidth = 10;
          ctx.lineCap = 'round';
          ctx.strokeStyle = color;

          if ('row' == winner.how) {
            ctx.moveTo(s/4, s/2 + s * winner.where);
            ctx.lineTo(s * 3 - s / 4, s/2 + s * winner.where);
          }
          else if ('col' == winner.how) {
            ctx.moveTo(s/2 + s * winner.where, s/4);
            ctx.lineTo(s/2 + s * winner.where, s * 3 - s / 4);
          }
          else if ('ulbr' == winner.how) {
            ctx.moveTo(s/4, s/4);
            ctx.lineTo(s * 3 - s / 4, s * 3 - s / 4);
          }
          else {
            ctx.moveTo(s/4, s * 3 - s / 4);
            ctx.lineTo(s * 3 - s / 4, s/4);
          }

          ctx.stroke();
        },

        hitTest: function(e) { 
          return {
            row: Math.floor(e.offsetY / Graphics.mainCellSize),
            col: Math.floor(e.offsetX / Graphics.mainCellSize),
          };
        }
      };

      var UI = {
        reset: function() {
          UI.game = new Game;

          $("#winner").css("display", "none");
          $("#newgame").css("display", "none");
          $("#instructions").css("display", "block");

          Graphics.draw(UI.game.getState(), null);

          UI.getBoard().click(UI.move);
        },

        getBoard: function() {
          return $("#board");
        },

        move: function(e) {
          var canvas = UI.getBoard();
          UI.lock(canvas, "wait");

          $("#instructions").css("display", "none");          
          var rowcol = Graphics.hitTest(e);

          if (!UI.game.move(rowcol.row, rowcol.col, UI.userMoveSaved)) {
            alert("Bogus move, broski");
            UI.unlock(canvas);
          }
        },

        lock: function(canvas, cursor) {
          canvas.css("cursor", cursor);
          canvas.unbind("click");
        },

        unlock: function(canvas) {
          canvas.css("cursor", "pointer");
          canvas.click(UI.move);
        },

        userMoveSaved: function(winner) {
          var game = UI.game;
          var ai = $('input[name=ai]:checked').val();

          Graphics.draw(game.getState(), winner);

          if (null == winner) {
            game.getAIMove(ai, UI.aiMoveSaved);
          }
          else {
            UI.gameOver(winner);
          }
        },

        aiMoveSaved: function(winner) {
          Graphics.draw(UI.game.getState(), winner);

          if (null == winner) {
            UI.unlock(UI.getBoard());
          }
          else {
            UI.gameOver(winner);
          }
        },

        gameOver: function(winner) {
          var message = {};
          message[Game.States.Human] = "You Win :)",
          message[Game.States.Opponent] = "You Lose :(",
          message[Game.States.Tie] = "Tie :|",

          $('#winner').text(message[winner.who]);
          UI.lock(UI.getBoard(), "default");

          $("#newgame").css("display", "block");
          $("#winner").css("display", "block");
        }
      };

      $(document).ready(function() {
        $('#newgame').click(UI.reset);
        UI.reset();
      });
    </script>

    <style>
      body {
        margin: 0;
      }

      div {
        width: 800px;
        margin: 0 auto;
      }

      #board {
        cursor: pointer;
        margin: 0px auto;
        display: block;
      }

      #header {
        width: 100%;
        background-color: #333;
        overflow: hidden;
        color: #fff;
        padding: 30px 0 30px;
      }

      h1 {
        text-align: center;
        font-size: 25px;
      }

      h2 {
        text-align: center;
      }

      h3 {
        margin-bottom10px;
        padding-bottom: 10px;
        border-bottom: 1px solid #ccc;
      }

      img {
        display: none;
      }

      #newgame { 
        cursor: pointer;
        text-decoration: underline;
        color: #44F;
      }

      #game {
        width: 500px;
        overflow: hidden;
        margin: 0;
        float:left;
        margin: 75px 0 40px;
      }

      #sidebar {
        width: 250px;
        height: <%= grid_size %>px;
        float: right;
        overflow: hidden;
        display: inline;
        margin-top: 75px;
        border-left: 1px solid #ccc;
        padding-left: 20px;
      }

      .clear {
        clear: both;
      }

      .notop {
        margin-top: 0px;
      }

      #history {
        overflow-y: hidden;
        width: 250px;
        height: 250px;
      }

    </style>

    <div id="header">
      <div>
	    <h1>Backbone / Rails Tic-Tac-Toe Minimax Machine</h1>
      </div>
    </div>

    <div>
      <div id="game">
        <canvas id="board" width="<%= grid_size %>" height="<%= grid_size %>"></canvas>
      </div>
      <div id="sidebar">
        <h3 class="notop">AI</h3>

        <input type="radio" value="Minimax" name="ai" checked="checked" />
        <label for="Minimax">Minimax</label>

        <input type="radio" value="FirstAvailable" name="ai"/>
        <label for="FirstAvailable">First Open</label>

        <h3>Move History</h3>
        <div id="history"><canvas id="history_canvas" width="<%= mini_grid_size %>" height="<%= 9 * (mini_grid_size + 20) %>"></canvas></div>
      </div>

      <div class="clear"></div>

      <h2 id="winner"></h2>
      <h2 id="instructions">Place an <em>X</em> by clicking the grid to start</h2>
      <h2 id="newgame">Click here to start a new game</h2>
    </div>

    <%= image_tag "x.png", :id => "x-img" %>
    <%= image_tag "o.png", :id => "o-img" %>
